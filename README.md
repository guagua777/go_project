# go_project


## 参考：
1. https://github.com/xpengkang/go-web-zero
2. 


## go 特性总结
1. 函数：
    1. => 作为普通的值
        1. 可以作为入参，函数作为参数传入另一个函数中，实现高阶函数、统一拦截、日志等
        2. 可以作为出参，函数作为返回值，从另一个函数中返回，
            1. 产生一个新的函数，这样可以实现类似于链式调用的功能
            ```golang
            参考 chain文件夹中代码
            ```
        3. 可以作为某个服务（或者是应用），以参数的形式传入另一个方法，
    2. => 接口，本质上就是函数，实现了函数，也就实现了接口
    3. => 方法表达式，即将某个具体类型的方法变为函数，可以实现批量调用、拦截、日志等
        1. 将具体类型的方法变为函数，变为函数后，就可以传入另一个方法中；可以以值的形式，传入框架中，实现批量调用、拦截、日志等
    4. => 函数类型，函数类型就是函数，函数类型的调用，就是函数的调用；给函数类型添加方法，添加方法后，如果该方法为某个接口的方法，即实现了该接口，方法中函数类型的调用，就是函数的调用
        1. 链式调用
        2. 实现框架的基石，为什么基于这个就能实现框架？

           什么是框架？

           只有架子，没有实现，那怎么把这个架子搭起来？
           1. 把方法作为入参，传递给另一个方法，这样就搭建起了架子，其中的这个方法，可以从外面传入（即具体的实现）
           2. 给类型挂载方法，这样就把这个架子搭起来了。
        
           框架的本质：给类型挂载方法，这样这个方法就可以从外面传入，实现框架和具体实现的分离

           有了函数类型后，函数类型挂载方法，用于实现骨架；而具体的方法由外面传入，用于实现具体的业务逻辑
         ```golang
        type HandlerFunc func(ResponseWriter, *Request)

        // ServeHTTP calls f(w, r).
        func (f HandlerFunc) ServeHTTP(w ResponseWriter, r *Request) {
            f(w, r)
        }
        ```
        1. 以及参考 method-expression-chain-invoke-type-fun 中的代码
    5. => 本质是什么，本质就是，函数做为值，可以传来传去；作为消息传来传去，就跟电路图中的电一样，可以传来传去。
    6. => 传递函数
2. goroutine
    1. => 本质上为一个continuation，
    2. => continuation + 调度器
    3. continuation是个表达式，是个计算，把它作为函数体，变为一个无参函数，函数是可以作为值的，把这个函数，加入到队列中
3. Context：闭包，解释器的env
    1. => 存值
    2. => 存channel，使用api的形式来完成，用于控制goroutine的执行流程
4. channel
    1. => 无缓冲通道，有缓冲通道
    2. => 输入具体的值
    3. => 输入空结构体 struct{}
    4. => 输出具体的值
    5. => 输出空结构体 struct{}，用作信号
    6. 

5. sync 
    1. => Mutex
    2. => RWMutex
    3. => WaitGroup
    4. => errgroup
    5. => once
    6. => condition
    7. => semaphore
    8. => semaphoregroup
    9. => sync.Map
    10. => sync.Once
    11. => sync.WaitGroup
